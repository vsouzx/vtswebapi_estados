
spring:
  application:
    name: vtswebapi_estados
  ## Profile Config
  config:
    use-legacy-processing: true
---
## Exemplo de Configuração
spring:
  cloud:
    config:
      ## Habilitar ou Não o Spring Cloud Config Client (False = desabilita)
      enabled: false